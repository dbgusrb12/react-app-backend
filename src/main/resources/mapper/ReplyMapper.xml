<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.reactappbackend.mapper.ReplyMapper">

    <select id="replyList" parameterType="Integer" resultType="Reply">
        SELECT REPLY_ID, R.USER_ID, U.USER_NAME, PARENT_ID, CONTENT,
                (SELECT COUNT(*) FROM REPLY WHERE REPLY_TYPE = 1 AND REPLY_ID = R.PARENT_ID) AS commentCount,
                CREATE_DATE, UPDATE_DATE
        FROM REPLY R
        JOIN USER U ON(R.USER_ID = U.USER_ID)
        WHERE REPLY_TYPE = 0
    </select>

</mapper>